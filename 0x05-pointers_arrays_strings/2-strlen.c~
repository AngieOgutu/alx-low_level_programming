#include <stdio.h>
#include "main.h"

/**
 * _strlen - Returns the length of a string
 * @s: string whose length is to be determined
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}

	return (i);
}
