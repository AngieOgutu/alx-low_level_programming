#include <stdio.h>
#include "main.h"

/**
 * main - returns largest integer
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */

int main ()
largest = largest_number (a, b, c);
{
	int a, b, c;

	if (a >= b && b >= c)

	{
		largest = a;
	}

	else if (b >= a && a >= c)

	{
		largest = b;
	}

	else

	{
		largest = c;
	}

	return (largest);

}
