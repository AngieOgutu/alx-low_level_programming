#include "main.h"

/**
 * _islower - checks for lower case charachter
 * @c : character to check
 * Return: (1) if lowercase, (0) if otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
