#include "main.h"

/**
 * _islower - checks for lower case charachter
 * @c : character to check
 * Return : (0) if lowercase, (1) if otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
